import styles from "./Subscription.module.scss";

const Subscription = () => {
  return (
    <footer className={styles.subscription}>
      <div className="container">Subscription</div>
    </footer>
  );
};

export default Subscription;
